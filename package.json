{
  "name": "motabhaai-backend-v1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "mocha tests/**/*.js -r dotenv/config",
    "test2": "env-cmd mocha tests/**/*.js"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "dotenv": "^10.0.0",
    "env-cmd": "^10.1.0",
    "mocha": "^8.4.0"
  }
}
